# Sandbox Runner

This separately deployable service will implement the C6 execution boundary.
It has no business-provider credentials and no direct access to the core
database. Inputs and outputs use immutable artifacts and signed task manifests.
