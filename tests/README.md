# Cross-System Tests

This directory owns tests that cross application boundaries:

```text
contracts/
e2e/
security/
load/
recovery/
golden/
```

Domain unit tests remain next to their Python or Vue packages. C12 acceptance
collects both local and cross-system results into one signed report.
